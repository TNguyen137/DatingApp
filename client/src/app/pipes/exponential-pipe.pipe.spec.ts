import { ExponentialPipePipe } from './exponential-pipe.pipe';

describe('ExponentialPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ExponentialPipePipe();
    expect(pipe).toBeTruthy();
  });
});
